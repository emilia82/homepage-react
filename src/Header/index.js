import "./style.css";

const Header = ({ title, subTitle }) => (
    <>
    <header>
    <h1>{title}</h1>
      </header>
  <subHeader>
<h2>{subTitle}</h2>
  </subHeader>
    </>
);

export default Header; 

