import "./style.css";

const Header = ({ title, subTitle }) => (
    <>
    <header>
    <h1>{title}</h1>
      </header>
  <sub__header>
<h2>{subTitle}</h2>
  </sub__header>
    </>
);

export default Header; 

